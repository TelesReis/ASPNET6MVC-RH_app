@model IEnumerable<MvcMovie.Models.Vaga>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nome)
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nome)
            </td>
            <td>
                <a asp-action="Relatorio" asp-route-id="@item.vaga_id">Relatorio</a> |
                <a asp-action="Edit" asp-route-id="@item.vaga_id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.vaga_id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.vaga_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
